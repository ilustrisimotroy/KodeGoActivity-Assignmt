/* 
    A. LOOP STATEMENT
*/
    let inputNumber = prompt(`Enter number: `);

        // function theNumber(inputNumber) {
        //     for(inputNumber; inputNumber <= 50; inputNumber++) {
        //         console.log(`Currently the number of ${inputNumber}. Terminating the loop`);
        //     }

        //     return theNumber;
        // }

        // theNumber(inputNumber);

